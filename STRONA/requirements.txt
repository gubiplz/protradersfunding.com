# Zależności dla hostingu bezserwerowego (Vercel czyta requirements.txt Z KORZENIA).
#
# To NIE jest pełna lista aplikacji — pełna jest w backend/requirements.txt.
# Różnica jest jedna i celowa: nie ma tu `playwright`. Provisioning realnych kont
# MT5 potrzebuje Chromium (~150 MB), czego na Vercela się nie wgra, a importy
# Playwrighta w kodzie są leniwe (wewnątrz funkcji), więc bez pakietu aplikacja
# startuje normalnie — po prostu trzymaj MT5_PROVISIONING=false.
#
# Lokalnie i na zwykłym serwerze instaluj z backend/requirements.txt.

fastapi>=0.110
sqlalchemy>=2.0
jinja2>=3.1
python-multipart>=0.0.9
pydantic>=2.6
python-dotenv>=1.0
itsdangerous>=2.1
segno>=1.6
stripe>=7.0
psycopg[binary]>=3.1
pywebpush>=2.0
