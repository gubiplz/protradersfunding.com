# Zależności dla hostingu bezserwerowego (Vercel czyta requirements.txt Z KORZENIA).
#
# To NIE jest pełna lista aplikacji — pełna jest w backend/requirements.txt.
# Playwright jest tu tylko jako klient CDP: Chromium (~150 MB) NIE jest instalowane.
# Realne konta MT5 otwieramy na Browserless (BROWSER_CDP_URL) — bez tej zmiennej
# panel i tak pokaże, że generator jest niedostępny.
#
# Lokalnie i na zwykłym serwerze (lokalne Chromium): backend/requirements.txt
# + `playwright install chromium`.

fastapi>=0.110
sqlalchemy>=2.0
jinja2>=3.1
python-multipart>=0.0.9
pydantic>=2.6
python-dotenv>=1.0
itsdangerous>=2.1
segno>=1.6
stripe>=7.0
psycopg[binary]>=3.1
pywebpush>=2.0
# Baza stref czasowych dla zoneinfo (Payout BOT liczy okno w America/New_York);
# na odchudzonym obrazie funkcji /usr/share/zoneinfo może nie istnieć.
tzdata>=2024.1
# Klient CDP do Browserless — bez lokalnego Chromium.
playwright>=1.40
